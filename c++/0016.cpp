#include"0016.h"
#include "limits.h"
using namespace std;

int Solution0016 ::threeSumClosest(vector<int> &nums, int target) {
    if(nums.size() < 3)
        return {};
    sort(nums.begin(),nums.end());
    int result,minimum = INT_MAX;
    for(int i = 0;i < nums.size();i++) {

    }
    return result;
}