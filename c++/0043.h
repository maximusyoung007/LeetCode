#include <string>
using namespace std;
class Solution {
public:
    string multiply(string num1, string num2);

    string multiplyToOne(string num1,char num2);

    string add(string num1,string num2);
};